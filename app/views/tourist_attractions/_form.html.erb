<%= form_for(@tourist_attraction) do |f| %>
  <% if @tourist_attraction.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@tourist_attraction.errors.count, "error") %> prohibited this tourist_attraction from being saved:</h2>

      <ul>
      <% @tourist_attraction.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
